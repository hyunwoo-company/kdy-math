# Gemini CLI 안내

이 저장소의 컨벤션은 **`AGENTS.md`** 에 있다. 작업 전에 그 파일을 읽고 규칙을 그대로 따를 것.

추가로, UI(페이지·섹션·컴포넌트·색·여백·타이포·애니메이션)를 만들거나 고칠 때는
**`.claude/skills/apple-design/SKILL.md` 를 직접 열어 읽어야 한다.**
이 디자인 시스템은 Claude Code에서만 자동 로드되며, Gemini CLI에서는 자동으로 주어지지 않는다.

규칙은 여기에 복제하지 않는다. 항상 `AGENTS.md` 가 최신 원본이다.
