# CONTENT-GUIDE.md — 사이트 글·사진 고치는 방법

이 문서는 **강사님(비개발자)** 을 위한 안내다. 코드를 직접 쓸 필요 없이, **AI 코딩 도구에게 말로 시키면** 된다.
(Claude Code / ChatGPT Codex / Gemini CLI / Cursor 등 무엇이든 상관없다.)

---

## 0. 기억할 세 가지

1. **글은 전부 `content/` 폴더 안에만 있다.** AI가 다른 곳을 고치겠다고 하면 "글은 content 폴더에만 있어야 해"라고 지적한다.
2. **디자인은 정해진 규칙이 있다.** AI에게 처음 시킬 때 이렇게 붙여주면 좋다.
   > "작업 전에 이 저장소의 `AGENTS.md` 를 읽고 그 규칙을 지켜줘."
3. **AI 말을 그대로 믿지 말고 눈으로 확인한다.** 아래 4번의 "확인하는 방법"을 쓴다.

---

## 1. 이렇게 말하면 된다 (예시 프롬프트)

각 예시 아래에 **AI가 실제로 고쳐야 하는 위치**를 적어 두었다. AI가 엉뚱한 파일을 고쳤다면 되돌리게 하면 된다.

### 홈 화면의 큰 문구를 바꾸고 싶다
> "홈 화면 제일 위 큰 문구를 '실력을 키우고 자신감을 완성하는 수학'으로 바꿔줘."

→ `content/` 안의 강사 정보 파일에서 **`instructor.headline`** 값. (현재 파일: `content/profile.ts`)
→ 두 줄로 나누고 싶으면 "두 줄로 나눠서" 라고 덧붙인다.

### 큰 문구 아래 설명글을 바꾸고 싶다
> "큰 문구 아래 설명 문장을 이렇게 바꿔줘: (원하는 문장)"

→ **`instructor.subheadline`**

### 전화번호·이메일을 바꾸고 싶다
> "전화번호를 010-1234-5678로, 이메일을 abc@example.com으로 바꿔줘. 눌렀을 때 전화 걸리는 링크도 같이 맞춰줘."

→ **`contact.phone` / `contact.phoneHref` / `contact.email` / `contact.emailHref`**
→ `phoneHref` 는 하이픈을 뺀 `tel:01012345678` 형태여야 한다. 링크가 안 맞으면 눌러도 전화가 안 걸린다.

### 상담 가능 시간이나 위치를 바꾸고 싶다
> "상담 시간을 '평일 15:00~22:00'으로 바꿔줘."

→ **`contact.hours`**, 위치는 **`contact.location`**

### 학부모용 안내에 항목을 하나 추가하고 싶다
> "학부모 안내 표에 '상담 방식: 전화 또는 방문 상담' 항목을 하나 추가해줘."

→ **`classInfo`** 목록에 `{ label, value }` 한 줄 추가. 화면의 표는 자동으로 늘어난다.

### 자주 묻는 질문(FAQ)을 추가·수정하고 싶다
> "FAQ에 '수업은 몇 명까지인가요?' 질문과 답변을 추가해줘."

→ **`faqs`** 목록에 `{ q, a }` 한 줄 추가.

### 경력·학력을 고치고 싶다
> "경력에 '현) 대치 시그랩스 수학'을 맨 위에 넣어줘."

→ **`career`** 목록. 최신 항목이 위로 오게 정렬한다.

### 커리큘럼 내용을 바꾸고 싶다
> "고3 과정 설명을 이렇게 바꿔줘: (원하는 문장)"

→ **`curriculum`** 목록에서 해당 학년 항목의 `body`.

### 숫자 지표(경력 n년 등)를 바꾸고 싶다
> "지표를 '12년 / 1,400명 / 1:6' 대신 실제 값으로 바꿔줘."

→ **`stats`** 목록. 3개를 유지하는 것이 디자인상 가장 보기 좋다.

### 수업 영상(유튜브)을 추가하고 싶다
> "수업 영상 탭에 유튜브 영상 3개 추가해줘. 링크는 이거야: (링크1) (링크2) (링크3)"

→ 영상 목록 파일(예: `content/videos.ts`)의 배열에 `{ title, youtubeId, description }` 형태로 추가.
→ **유튜브 링크 전체가 아니라 영상 ID만** 들어간다(`https://youtu.be/**abc123XYZ**` 의 굵은 부분). AI가 알아서 변환해 준다.
→ 영상이 하나도 없으면 "준비 중" 화면이 나오고, 배열에 추가하면 자동으로 영상 목록이 생긴다.

### 수업 후기를 추가하고 싶다
> "수업 후기에 이 메시지를 추가해줘: (받은 메시지 원문). 보낸 사람은 학생이야."

→ `content/reviews.ts` 의 **`quotes`** 목록에 `{ label, quote, from }` 한 줄 추가.
→ `label` 은 "18점 상승", "수학 100점" 처럼 결과를 한눈에 보여주는 짧은 문구. `from` 은 **"학생" / "학부모님" / "졸업생"** 중 하나만 쓴다.
→ **받은 문장을 다듬지 말고 그대로 넣는다.** 오타나 줄임말이 있어야 진짜 받은 말처럼 읽힌다.
→ ⚠️ **실명·학교명·연락처는 절대 넣지 않는다.** 문장 안에 이름이 있으면 "아이" 처럼 바꾼다.

### 후기 카톡 캡쳐를 추가하고 싶다
> "후기 캡쳐를 추가해줘. 원본은 `_source-images/reviews/` 에 넣어뒀어."

→ AI가 `scripts/resize-review-images.js` 의 목록에 추가하고 실행해서 `public/images/reviews/` 에 웹용 파일을 만든 뒤, `content/reviews.ts` 의 **`shots.items`** 에 등록한다.
→ ⚠️ **올리기 전에 캡쳐를 직접 눈으로 확인하라.** 아래가 보이면 그 캡쳐는 쓰지 않는다.
  - 학생·학부모 **이름**, 전화번호, 학교 이름
  - **합격증·성적표 원본** (수험번호와 성명이 찍혀 있다)
  - 얼굴이나 풍경이 보이는 **프로필 사진**
→ 카카오톡 기본 회색·파란 아이콘, "쌤"/"학생" 표시, 시간(오후 3:34)은 괜찮다.
→ **후기와 캡쳐는 학생·학부모님이 보낸 것이다.** 사이트에 공개하기 전에 동의를 받아 두는 것이 원칙이다.

### 홈에 보이는 후기 3개를 바꾸고 싶다
> "홈에 보이는 후기를 다른 걸로 바꿔줘. (원하는 후기 내용)"

→ `content/reviews.ts` 의 **`homeSection.quoteIndexes`** 숫자를 바꾼다. `quotes` 목록의 순번(맨 위가 0)이다.

### 프로필 사진을 새 사진으로 바꾸고 싶다
> "새 프로필 사진을 홈 화면 사진으로 바꿔줘. 원본 파일은 `_source-images/새사진.jpg` 에 넣어뒀어. 웹용으로 리사이즈해서 넣어줘."

→ 원본은 `_source-images/` 에 두고, AI가 **긴 변 1600px / JPEG 품질 85** 로 줄여 `public/images/` 에 저장한다.
→ 사진 위치별 파일: 홈 히어로 `kdy-hero.jpg` / 소개 `kdy-about.jpg` / 수업·상담 `kdy-teaching.jpg`.
→ ⚠️ **원본(10MB짜리)을 그대로 넣지 말라**고 못 박아 둘 것. 사이트가 느려진다.

### 탭 이름이나 순서를 바꾸고 싶다
> "상단 메뉴에서 '수업 영상'을 '강의 영상'으로 바꿔줘."

→ 네비게이션 항목 목록(**`navItems`**)의 `label`. 링크 주소(`href`)는 건드리지 않는다.

### 브라우저 탭에 뜨는 제목을 바꾸고 싶다
> "브라우저 탭 제목을 'DYnamic MATH | 김동영 수학'으로 바꿔줘."

→ **`site.title`**, 검색 결과에 나오는 설명은 **`site.description`**

---

## 2. 이런 건 부탁하지 말 것 (또는 개발자와 상의)

| 요청 | 이유 |
|---|---|
| "문의 폼(이름·연락처 입력칸) 만들어줘" | 서버가 없어서 접수가 불가능하다. 전화·이메일 링크로만 운영한다 |
| "색을 좀 더 화려하게 / 빨간색으로 바꿔줘" | 색 규칙이 정해져 있다(포인트 색은 파란색 `#0071e3` 하나). 바꾸려면 `AGENTS.md` 규칙부터 함께 고쳐야 한다 |
| "글씨를 훨씬 두껍게" | 사용 가능한 굵기가 정해져 있다 |
| "애니메이션 화려하게 넣어줘" | 등장 효과만 쓴다. 무한 반복 애니메이션은 금지 |

정말 바꾸고 싶으면 "왜 이 규칙이 있는지 `AGENTS.md` 를 읽고, 규칙까지 함께 수정하는 방안을 제안해줘"라고 시킨다.

---

## 3. 수정한 내용이 사이트에 반영되는 과정

```
1) AI에게 수정을 시킨다
2) AI가 content/ 파일을 고친다
3) AI가 검증한다        →  npm run build / npx tsc --noEmit / npm run lint
4) 커밋 & 업로드        →  git commit + git push (main 브랜치)
5) 자동 배포             →  Vercel 이 알아서 배포
6) 1~3분 후 https://kdy-math.vercel.app 반영
```

**5번은 사람이 할 일이 없다.** `push` 만 되면 나머지는 자동이다.

AI에게 3~4번까지 한꺼번에 시켜도 된다.

> "고친 다음에 `npm run build`, `npx tsc --noEmit`, `npm run lint` 세 개를 실행해서 통과하는지 확인하고, 결과를 보여줘. 그다음 커밋해서 push해줘. 커밋 메시지는 한국어로."

⚠️ **3번(검증)을 건너뛰지 말고 꼭 시켜라.** 예전에는 업로드 후에 한 번 더 자동으로 검사해 주는 장치가 있었는데, 지금은 없다. 잘못된 코드를 올리면 배포가 실패하고 사이트는 이전 상태로 남는다(망가지지는 않는다).

배포가 잘 됐는지 보려면 **Vercel 대시보드**(팀 `jenu8628s-projects` → 프로젝트 `kdy-math` → Deployments)를 연다.
- **Ready** ✅ → 배포 완료
- **Error** ❌ → 코드에 오류가 있어 배포가 **멈췄다.** 사이트는 이전 상태 그대로이니 망가지지 않는다. AI에게 "Vercel 배포가 실패했어, 빌드 로그 보고 고쳐줘"라고 하면 된다.

---

## 4. 확인하는 방법 (AI 말을 검증하기)

1. **로컬에서 눈으로 본다** — 터미널에서 `npm run dev` 실행 후 브라우저에서 http://localhost:3000 열기. 끄려면 `Ctrl + C`.
2. **어느 파일을 고쳤는지 물어본다** — "어떤 파일의 어떤 값을 고쳤는지 알려줘." 답이 `content/` 안의 파일이면 정상이다.
3. **검증 3종 결과를 요구한다** — "빌드 통과했어?" 라고 묻지 말고 **"명령을 실행한 출력 결과를 보여줘"** 라고 한다.
4. **배포 후 실제 주소를 확인한다** — https://kdy-math.vercel.app 에서 확인. 옛 화면이 보이면 `Ctrl + Shift + R` 로 강력 새로고침.

## 5. 잘못 고쳤을 때 되돌리기

- **아직 push하지 않았다면**: "방금 수정한 것 전부 되돌려줘(git으로 원복)."
- **이미 배포됐다면**: "직전 커밋으로 되돌리고 push한 다음, 배포 명령까지 실행해줘." 1~2분 뒤 사이트가 이전 상태로 돌아온다.
- **가장 빠른 방법**: Vercel 대시보드(팀 `jenu8628s-projects` → 프로젝트 `kdy-math`)의 배포 목록에서 이전 배포를 골라 **Rollback**. 코드를 건드리지 않고 즉시 되돌아간다. 단 이건 화면만 되돌리는 것이므로, 코드도 원복해두지 않으면 다음 배포에서 문제가 다시 나타난다.

## 6. 아직 비어 있는 정보

아래는 실제 정보를 받지 못해 **임시 값(더미)** 이 들어가 있다. 오픈 전에 실제 값으로 바꿔야 한다.

- 전화번호, 이메일, 카카오톡 채널, 상담 가능 시간, 위치
- 수강료 및 수업 운영 세부 정보
- 수업 영상 (촬영 후 등록)
- 명함용 QR 코드 (최종 주소 확정 후 제작)
